{
  "name": "@amplifi/research",
  "version": "0.1.0",
  "description": "Backtest + scenario research harness over strategy-core and quant-core.",
  "type": "module",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "test": "tsx test/verify.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@amplifi/strategy-core": "0.1.0" },
  "devDependencies": { "@types/node": "^22.9.0", "tsx": "^4.19.2", "typescript": "^5.6.3" }
}
