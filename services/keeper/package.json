{
  "name": "@amplifi/keeper",
  "version": "0.1.0",
  "description": "Off-chain keeper agent: drives the strategy state machine and writes through a chain client (in-memory mock or viem).",
  "type": "module",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "oracle": "tsx src/oracleLoop.ts",
    "test": "tsx test/verify.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@amplifi/strategy-core": "0.1.0",
    "@amplifi/svc-kit": "0.1.0",
    "viem": "^2.21.45"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
